server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/security
    username: cos1234
    password:

logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG




